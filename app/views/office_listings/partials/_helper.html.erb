
<script>

$(function(){

	office_map.display_map(<%= neighborhood.latitude %>, <%= neighborhood.longitude %>);

	<% office_listings.each do |office_listing| %>
	office_map.add_marker(<%= office_listing.latitude %>, <%= office_listing.longitude %>, "<%= office_listing.address %>")
	<% end %>

});
</script>